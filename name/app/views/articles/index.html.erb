<h3>Listing tickets prototype</h3>
<%= link_to 'New Ticket', new_article_path %>
<hr />

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Customer</th>
    <th>User</th>
    <th></th>
  </tr>

  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.description %></td>
      <td><%= article.customer %></td>
      <td><%= article.user %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
    </tr>
  <% end %>
</table>